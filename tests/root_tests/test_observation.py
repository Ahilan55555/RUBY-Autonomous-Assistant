from core.observation import Observation

observation = Observation()

print(observation)

print(type(observation).__name__)

print(observation.url)

print(observation.page_title)

print(observation.visible_text)